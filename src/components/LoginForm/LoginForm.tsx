import { Box, Card, Container } from '@mui/material';

const LoginForm = (): JSX.Element => (
  <Container>
    <Box my={10}>
      <Card variant="outlined">Login form</Card>
    </Box>
  </Container>
);

export default LoginForm;
